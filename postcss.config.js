export default {
  plugins: {
    // Tailwind CSS - genera estilos basados en las clases que usamos
    tailwindcss: {},
    
    // Autoprefixer - añade compatibilidad automática con navegadores
    // Ej: -webkit-, -moz-, -ms- para que funcione en Chrome, Firefox, Safari, etc.
    autoprefixer: {},
  },
}